# 推荐系统 (Referral System)

一个基于以太坊钱包的一层推荐系统，支持团队管理和统计功能。

## 功能特性

- 🔗 MetaMask钱包连接
- 👥 一层推荐关系绑定
- 📊 实时统计数据和排名
- 🎯 团队管理后台（邮箱登录）
- 🏆 推荐人排名榜
- 📋 一键复制钱包地址
- 📊 导出CSV表格
- 💾 防重复绑定机制
- 🚀 支持Vercel部署

## 技术栈

- **前端**: Next.js 14, React, TailwindCSS
- **API**: Next.js API Routes
- **数据库**: SQLite (本地) / Vercel Postgres (生产)
- **Web3**: ethers.js, MetaMask
- **图标**: Lucide React

## 安装

```bash
npm install
```

## 运行

1. 启动后端服务器:
```bash
npm run server
```

2. 启动前端开发服务器:
```bash
npm run dev
```

3. 访问应用:
- 前端: http://localhost:3000
- 后台管理: http://localhost:3000/admin
- 统计页面: http://localhost:3000/stats
- 后端API: http://localhost:3001

## 📦 本地开发

### 安装依赖
```bash
npm install
```

### 启动开发服务器
```bash
npm run dev
```

### 访问应用
- 主页: http://localhost:3000
- 后台管理: http://localhost:3000/admin
- 统计页面: http://localhost:3000/stats

## 🚀 部署到Vercel

### 方式1: 通过GitHub（推荐）

1. **推送代码到GitHub**
```bash
# 使用提供的批处理文件
推送到GitHub.bat

# 或手动执行
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/NYCTEAM/REF.git
git branch -M main
git push -u origin main
```

2. **在Vercel部署**
- 访问 https://vercel.com
- 点击 "New Project"
- 导入 GitHub 仓库 `NYCTEAM/REF`
- 点击 "Deploy"

### 方式2: 使用Vercel CLI
```bash
npm i -g vercel
vercel login
vercel --prod
```

### ⚠️ 数据库说明

**当前版本使用SQLite，在Vercel上有限制：**
- ✅ 可以部署和运行
- ❌ 数据不会持久化（每次部署重置）
- ⚠️ 仅适合演示，不适合生产环境

**生产环境建议：**
- 使用 Vercel Postgres
- 使用 Supabase
- 使用 PlanetScale

详见 `VERCEL部署说明.md`

## 使用流程

### 管理员操作

1. 访问后台管理页面: `http://localhost:3000/admin`
2. 使用邮箱登录（测试账号: admin@example.com / admin123）
3. 输入团队名称创建团队
4. 复制生成的推荐链接
5. 将推荐链接发送给团队长

### 用户操作

1. 用户通过团队长的推荐链接访问: `http://localhost:3000?ref=团队长钱包地址`
2. 页面显示团队长信息和邀请
3. 用户点击"连接MetaMask钱包"
4. 用户点击"确认绑定"完成加入团队
5. 绑定成功后，关系自动存入数据库

### 查看统计

访问统计页面: `http://localhost:3000/stats`
- 查看总用户数
- 查看各团队人数
- 查看所有钱包地址

## 数据库

使用SQLite存储推荐关系，数据库文件: `referrals.db`

## API接口

- `POST /api/bind` - 绑定推荐关系
- `GET /api/stats` - 获取统计数据
- `GET /api/user/:address` - 获取用户信息
